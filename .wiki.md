Project Summary
Prakriti is an intelligent agricultural advisory system aimed at supporting Indian farmers by providing contextual, multilingual advice on crops, pests, diseases, and farming practices. It leverages a Knowledge Graph integrated with a Retrieval-Augmented Generation (RAG) pipeline to deliver real-time, localized information that enhances agricultural productivity and sustainability.

Project Module Description
Frontend: Developed with React, featuring a chat interface for user queries and a graph visualization of agricultural knowledge. The application now includes proper branding and enhanced UI components.
Backend: Built using FastAPI, it connects to a Neo4j database, processes queries, and manages real-time updates through an autonomous agent.
Knowledge Graph: Utilizes Neo4j to store and manage agricultural data, enabling structured querying.
RAG Pipeline: Combines structured data from the Knowledge Graph with unstructured data from FAISS for improved query responses.
Directory Tree
agri-agent/
├── backend/                  # Backend application files
│   ├── main.py              # FastAPI application entry point
│   ├── config.py            # Configuration settings
│   ├── kg_connector.py       # Neo4j connection and data loading
│   ├── rag_pipeline.py       # RAG pipeline implementation
│   ├── agent_updater.py      # Autonomous agent for updates
│   ├── requirements.txt      # Python dependencies
│   └── .env                 # Environment variables
├── frontend/                 # Frontend application files
│   ├── src/                  # Source code for React app
│   ├── Dockerfile.frontend   # Dockerfile for frontend
│   └── package.json          # Frontend dependencies
├── docker-compose.yml        # Docker Compose configuration
└── README.md                 # Project documentation
File Description Inventory
backend/main.py: Entry point for the FastAPI application, managing endpoints and application lifecycle.
backend/config.py: Loads environment variables and validates configuration settings.
backend/kg_connector.py: Handles the connection to Neo4j and manages data loading from CSV files.
backend/rag_pipeline.py: Implements the retrieval-augmented generation pipeline.
backend/agent_updater.py: Manages autonomous updates for pest alerts and weather events.
frontend/src/: Contains React components for the chat interface and graph visualization, including the updated Index.tsx and App.jsx.
docker-compose.yml: Configuration for running the application stack using Docker.
README.md: Contains project overview, installation instructions, and usage guidelines.
Technology Stack
Backend: FastAPI, Neo4j, LangChain, FAISS, OpenAI
Frontend: React, Vite, Tailwind CSS
Database: Neo4j AuraDB
Deployment: Docker Compose
Usage
Installation
Clone the repository and navigate to the project directory.
Frontend Setup:
cd frontend
pnpm install
pnpm run build
Backend Setup:
cd backend
python -m venv venv
source venv/bin/activate  # On Windows use venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
# Edit .env with your Neo4j and OpenAI credentials
uvicorn main:app --reload
Docker Setup (Alternative):
docker-compose up --build
Running the Application
The frontend and backend services will run on their respective ports.